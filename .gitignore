# === Variables de entorno / secretos ===
.env
.env.local
.env.*.local
*.env

# === Datos reales de usuarios / planillas (NUNCA al repo) ===
# Los datos reales viven solo en Supabase. Usar la plantilla con placeholders.
*_usuarios_*.sql
# ...pero SÍ permitir plantillas/ejemplos sin datos reales:
!*_usuarios_*.example.sql
!*_usuarios_*.TEMPLATE.sql
*.xlsx
*.csv

# Nota: SQL de esquema/seed no se ven afectados (no contienen "_usuarios_"):
#   supabase_schema.sql, seed_modules.sql, supabase/*.sql

# === Build / dependencias ===
node_modules/
dist/
.vite/

# === Sistema / editor ===
.DS_Store
*.log
.idea/
.vscode/

# === Documentos de trabajo Cowork (NO versionar en el repo) ===
# Viven junto al repo en la carpeta de Google Drive, pero no pertenecen al código.
ANALITICA_NAVEGACION.md
HISTORIAL_PROYECTO.md
INDICADORES_CONSTELAR.md
REFERENCIA_VISUAL_NEBULOSA.md
REMEDIACION_LISTA.md
VERIFICACION_FASE0.md
FASE1_CAMBIOS.md
FASE3_EDITOR_PASO1.md
INFORME_AVANCE_*.md
gitignore_propuesto.txt
Traspaso/
Capturas iPad/

# === Referencias iStock (marca de agua / derechos): NUNCA subir al repo ===
Referencias/
